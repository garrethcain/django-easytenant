[project]
name = "easyshard-example-monolith"
version = "0.1.0"
description = "Monolith example for django-easyshard"
requires-python = ">=3.10"
dependencies = [
  "Django>=4.2",
  "django-easyshard",
  "PyJWT>=2.8.0",
]

[tool.uv.sources]
django-easyshard = { path = "../..", editable = true }
