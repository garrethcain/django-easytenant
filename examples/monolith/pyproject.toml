[project]
name = "easytenant-example-monolith"
version = "0.1.0"
description = "Monolith example for django-easytenant"
requires-python = ">=3.10"
dependencies = [
  "Django>=4.2",
  "django-easytenant",
  "PyJWT>=2.8.0",
]

[tool.uv.sources]
django-easytenant = { path = "../..", editable = true }
